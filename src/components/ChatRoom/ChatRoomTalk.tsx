import { ChatTalkType } from './ChatRoom.type';

function ChatRoomTalk({
  profileImg,
  petName,
  userName,
  talk,
  timestamp,
}: ChatTalkType) {
  return <div>ChatRoomTalk</div>;
}

export default ChatRoomTalk;
