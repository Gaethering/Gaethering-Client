import { Comment } from "../../api/boardAPI.type"

function Comment({  commentId,
  memberId,
  content,
  nickname,
  createdAt,}: Comment) {
  return (
    <div>Comment</div>
  )
}

export default Comment